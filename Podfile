# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VKAudioToken' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'ObjectMapper'
  # Pods for VKAudioToken

end

target 'VKToken_iOS' do
use_frameworks!
pod 'Alamofire'
pod 'ObjectMapper'
end
